# WinAgent standing goals — the daemon and idle-learner work through these.
# Lines under `## Learn queue` are one-shot topics (first line = next, popped).
# Standing goals repeat: pick the stalest skill or the oldest unchecked item.

## Standing goals
- Keep every skill in skills/ fresh: oldest file refreshes first.
- Re-verify the wordpress-build playbook monthly (WP moves fast).
- Track Chrome/Edge automation changes (Manifest/CDP deprecations).
- Collect Windows admin recipes that save real tasks (disk, startup, services).

## Learn queue
- What's new in Node.js LTS for automation scripts
