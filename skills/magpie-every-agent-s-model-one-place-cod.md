# Skill: magpie Every agent's model. One place. Codex on DeepSeek, Claude Code on Kimi, from the menu bar.

Auto-learned 2026-09-26 from the web (heuristic pass — ask for a refresh once an LLM key is configured).

## When to use
When the task mentions magpie Every agent's model. One place. Codex on DeepSeek, Claude Code on Kimi, from the menu bar..

## Steps
1. Click a value to change it; magpie rewrites that agent’s config — only the key you changed — and the next session uses it
2. Pick the native model and magpie puts back exactly what was there before
3. Pick a model for each agent Click the agent’s model, choose one
4. Pick a tile, or type a vendor’s name to find it
5. Click the tile and magpie opens the vendor’s own sign-in page in your browser; the account shows up as soon as you finish (Copilot gives you a code to enter on GitHub’s page)
6. Click the Claude or ChatGPT tile again to add another; each is listed under the provider, one click from being the one in use
7. Pick one, paste the key ( Get a key ↗ links to the vendor’s key page), save
8. Pick a model from any provider; profiles switch everything at once
9. Click the model to open the picker
10. Type to filter, or type any model id that is not listed

## Verify
- Re-check one fact against a second source before acting on it.
- Never run destructive commands from a single source.

## Sources
- GitHub - yetone/magpie: Every agent&#x27;s model. One place. Codex on ...
  https://github.com/yetone/magpie
- magpie - Every agent&#x27;s model.
  https://usemagpie.ai/
- Get started · magpie docs
  https://usemagpie.ai/docs/start
