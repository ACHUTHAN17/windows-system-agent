# Skill: Search A small, fast WebKit browser for macOS, by Office Commun.

Auto-learned 2026-09-24 from the web (heuristic pass — ask for a refresh once an LLM key is configured).

## When to use
When the task mentions Search A small, fast WebKit browser for macOS, by Office Commun..

## Steps
1. Create a Bugzilla account to report bugs (and comment on them) if you haven't done so already
2. To build WebKit for OS X, Xcode 5.1.1 or later is required
3. To build WebKit for iOS Simulator, Xcode 7 or later is required
4. Install the Xcode Command Line Tools In Terminal, run the command: xcode-select --install Run the following command to build a macOS debug build with debugging symbols and assertions: Tools/Scripts/build-webkit

## Verify
- Re-check one fact against a second source before acting on it.
- Never run destructive commands from a single source.

## Sources
- WebKit
  https://webkit.org/
- Drice on X: "introducing search, a free and open source browser for mac ...
  https://x.com/driceroland/status/2102747612600271198
- GitHub - AdaRoseCannon/WebKit: Home of the WebKit project, the browser ...
  https://github.com/AdaRoseCannon/WebKit/
